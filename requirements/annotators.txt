# EntitySeg/HED preprocessing. detectron2 and the deformable-attention op are
# compiled locally, so a working CUDA toolkit/compiler is required.
git+https://github.com/facebookresearch/detectron2.git@v0.6
